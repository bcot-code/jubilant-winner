<div class="Comments">
  {{#each this}}
    <section class="comment">
      <div class="meta">
        {{user.username}}
        on
        {{created_on}}
      </div>
      <div class="text-box">
        {{content}}
      </div>
    </section>
  {{/each}}
</div>